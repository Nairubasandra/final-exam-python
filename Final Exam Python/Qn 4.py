# 4 i)
# a)
# Object - Oriented Programming(OOP) refers the programming language that is used to define the occurance of different words 
# Significance in software development
# It helps to discover the class of an item
# It always has classes and objects

# b)
# A class is one with properties/attributes
# A class has attributes while An object takes on the properties of the class

# 4 ii)
class subject:
    def __init__(self,name,description):
        self.name = name
        self.description = description
p = subject('python','exam') 
print(p.name) 
print(p.description)


# 4 iii)
def alphabetical_letters(a,b):
    for letter in alphabetical_letters:
        sum==0
    return sum

# 4 iv)
class car:
    def __init__(self,brand,name,color):
        self.brand = brand
        self.name = name
        self.color = color
p = car('brand','name','color')
print(p.brand)
print(p.name) 
print(p.color)       
            
    

# 4 v)

class car:
    def __init__(self,brand,name,color,start_engine):
        self.brand = brand
        self.name = name
        self.color = color
        self.start_engine = start_engine
p = car('brand','name','color',start_engine='Engine_of_the_car_has_started')
print(p.brand)
print(p.name) 
print(p.color)
print(p.start_engine)       
        
        



